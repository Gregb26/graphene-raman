# NOTES_EPW_REPERES.md — repères durables du volet EPW (chapitre 5, couplage électron-phonon)

Anciennement `graphene/qe/epw/CLAUDE.md` (rapatrié dans le dépôt le 2026-09-21 ; l'ancien chemin est un lien symbolique
vers ce fichier). Voir `NOTES_EPW.md` pour l'état courant des jobs. Ce fichier ne contient que ce qui ne change pas.

- Codes : QE 7.5, EPW 6.0 (`module restore qe`). Tout en job SLURM (rrg-cotemich-ac) ; jamais sur le nœud de connexion.
- Chaîne : scf → ph.x (nq) → q2r/matdyn → `scripts/epw_pp_save.py` (pp.py absent du module) → save/ → nscf (liste de k crystal, nbnd 32) → epw1 (wannierize, epbwrite, epwwrite) → epw2 band_plot / prtgkk / elecselfen (epwread, liens vers ../epw1/{crystal,epwdata,dmedata,vmedata,wigner,selecq}.fmt, graphene.ukk, graphene.epmatwp, graphene.save).
- Grille de production : 24k-24q, degauss 0.002 Ry, ecutwfc 100 Ry, assume_isolated '2D', nbnd 32 ; fenêtre gelée dis_froz_max = E_D + 2.5 eV = −1.74 eV, dis_win −25/15 eV, proj C1:sp2;pz C2:pz (comme le chapitre 4).
- Décisions arbitrées : degauss 0.002 (écart avec le chapitre 4 à 0.01 Ry documenté, E_D identique −4.2389 eV) ; T = 300 et 10 K ; grilles fines 120² et 240² (360² seulement si écart > 5 %) ; fsthick 3.5 eV ; efermi_read avec fermi_energy = E_D.
- Conventions : Γ^ep = 2 Im Σ (largeur totale), Γ^ed = −2 Im Σ ; npz tagués `units='eV'` + `E_D` ; jamais de rescalage en sortie ; |g| DFPT vs EPW comparés par sous-espaces dégénérés (projection), jamais par index de bande ; figures en français, `figures/memoire.mplstyle`, PDF+PNG, données depuis npz ; chiffres bruts, sans jugement.
- Points : dans ph.x 7.5 le chemin q est en cartésien 2π/a (pas de `q_in_cryst_coord`) : K = (0.5773503, −0.3333333), M = (0.2886751, −0.5). En crystal (EPW/W90) : K = (2/3, 1/3) ; (1/3, 1/3) n'est pas K.
- Pièges rencontrés : epw1 — la tâche 0 rassemble epmatwp, prévoir ≥ 8 Go pour elle (OOM à 4 Go/cœur sur 24k-24q, puis blocage jusqu'au TIMEOUT ; relance avec epbread + wannierize=false via graphene.ukk en 2 min). `q_in_cryst_coord` et les lignes de commentaire longues dans la namelist font échouer ph.x. Le « Fermi energy fine k-mesh » d'epw1 à nkf = 1 est sans signification.
- P2 elecselfen (`24k-24q/epw2_selfen_<nkf>_T<T>_dg<dg>/`) : ne jamais lier selecq.fmt (EPW l'écrase à travers le lien) ; epw.out imprime E − E_F (remis en absolu une seule fois dans `electron_phonon/selfen.py`) ; la liste k irréductible d'EPW (mp_mesh_k) contient plus de points que le groupe complet (2461 pour 120², 1261 orbites) → poids reconstruits par orbite dans la base RÉCIPROQUE (b1·b2 < 0) ; seuls les états |ε − E_F| ≤ fsthick sont significatifs. Γ^ep(ε) = moyenne lorentzienne η = 0.02 eV, médianes sur ε (±3, ±1.2 eV), comme resonance_metrics.py.
- P6 phonselfen : mp_mesh_k = .false. obligatoire (q hors symétrie) ; gamma___ = 2 Im Π = largeur totale (FWHM), tranché par le test prtgkk sur l'anneau (P11, `scripts/epw_ring_check.py`, rapport 1.997 à Γ et K) ; HWHM = gamma___/2 ; l'anneau résonant π→π* (ħω/2v ≈ 0.01 Å⁻¹) impose nkf ≥ 960² (1200² retenu, σ 0.02) — 120²/240² donnent 0 ou +119 % ; ω(E2g,Γ) = 162.7 et ω(A1',K) = 120.5 meV dans cette chaîne (degauss 0.002), à citer tels quels. **A1' = mode le plus haut à K (branche 3 à degauss 0.002, branche 6 à 0.02)** : tout ce qui sélectionne A1' à K ou le doublet E2g à Γ le fait par caractère (plus grand γ ou plus grande somme de bloc π/π*) ou par fréquence (deux plus hauts, dégénérés), jamais par index de mode (`phself.mode_A1p`, `phself.modes_E2g`, P18 2026-09-21).
- Pseudo : C.upf ONCVPSP NC, PBE (md5 34a24e64…), identique au ch. 4. La bosse de Γ^ep(ε) à E_D est un artefact de la moyenne lorentzienne (DOS nulle), pas des états (§1f).
- P7 : ⟨D²⟩ (moyenne par entrée du bloc π/π*) = 43.6/86.3 (EPW) et 45.1/91.6 eV²/Å² (DFPT prt) ≈ Piscanec 2004 (45.6/92.05) ; le run DFPT « prt » tourne sur 16×16 k (K hors grille) et donne les ω physiques (194.2 / 159.8 meV) ; `scripts/epw_d2_extract.py`.
- Décision 2026-09-10 : EPW est une méthode validée ; pas de re-validation fine de |g| (bandes, phonons, décroissance suffisent). Trois figures pour le chapitre 5 (`scripts/make_figures_epw.py`), contrôle |g| optionnel.
- Formats : prtgkk → blocs `iq/ik`, colonnes ibnd jbnd imode enk enk+q omega |g_sym| |g| Re Im (`scripts/epw_extract_gkk.py`) ; band_plot → `band.eig`, `phband.freq` (format &plot, lus par `scripts/epw_validate.py`).
- Validation 16k-8q : son test |g| est invalide (chemin DFPT en crystal lu comme cartésien) ; ne pas s'y référer.
- Espace : les `_ph0/` ne sont supprimés qu'après arbitrage explicite de l'utilisateur (faits le 2026-09-10 : 24k-24q, 30k-30q ; restent 16k-16q, 12k-12q, 16k-8q).
